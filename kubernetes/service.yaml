apiVersion: v1
kind: Service
metadata:
  name: travel-price-api-service
spec:
  selector:
    app: travel-price-api
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: NodePort
